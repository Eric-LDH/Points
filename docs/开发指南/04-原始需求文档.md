
**角色设定：**
你是一位经验丰富的全栈开发工程师，擅长移动端应用开发（Vue3 + Uniapp/React Native + Expo）和本地数据存储管理，对现代化UI/UX设计有深刻理解。

**核心任务：**
请根据我提供的附件《家庭积分奖惩规则明细表（周计划）.xlsx》，开发一个**手机端积分管理系统**，要求具备：
- ✅ 现代化界面风格（清新卡片+微动画+深色模式）
- ✅ 手工补录往期积分（单日/批量）
- ✅ **积分规则自定义编辑功能（图标随机生成）**
- ✅ **兑换商品支持【每周/每月/每年】周期限制**
- ✅ **备份功能移入【我的】页面**
- ✅ **强化积分兑换功能（原备份Tab改为兑换）**

---

## 🎨 **一、界面风格要求（主流现代化设计）**

### 1. **设计风格**
```
🏆 整体风格：清新、明亮、卡片式设计
- 配色方案：主色#6366F1（Indigo）、辅色#10B981（翡翠绿）、点缀色#F59E0B（琥珀色）
- 字体：SF Pro / Inter / 系统默认无衬线字体
- 圆角：大圆角（16px卡片圆角，24px按钮圆角）
- 阴影：轻量阴影（0 4px 6px -1px rgba(0, 0, 0, 0.1)）
- 图标：线性图标，2px描边，圆润端点
```

### 2. **交互体验**
```
✨ 微交互动画：
- 按钮点击：涟漪效果 + 轻微缩放（0.98）
- 页面切换：平滑左右滑动
- 勾选完成：打勾动画 + 震动反馈（可选）
- 积分变化：数字滚动动画
- 规则编辑：拖拽排序 + 滑动删除
- 兑换操作：成功弹窗 + 积分扣除动画

🌙 深色模式支持：
- 自动跟随系统
- 手动切换开关
- 深色配色：#1F2937背景，#374151卡片
```

### 3. **底部导航结构调整**
```
底部导航栏（5个Tab，居中图标+文字）：
┌─────────────────────────────────┐
│ [🏠] [📊] [➕] [🎁] [👤]        │
│ 首页  统计  补录  兑换  我的     │
└─────────────────────────────────┘

说明：
- 原【备份】功能移入【我的】页面
- 【兑换】成为独立Tab（原备份位置）
- 中间【补录】按钮使用特殊样式（渐变背景+放大效果）
```

---

## 📝 **二、核心功能详细说明**

### 1. **首页 - 今日积分记录**（保持不变）
```
┌─────────────────────────────────┐
│ 早安，小明的家长 👋  2026.03.09 │
│        周一                      │
│ 今日积分: +28 │ 累计积分: 356    │
├─────────────────────────────────┤
│ 🔥 连续打卡 5 天                 │
├─────────────────────────────────┤
│ ⚡ 快捷记录                       │
│ [家务类] [习惯] [学习] [惩罚]    │
├─────────────────────────────────┤
│ 今日已完成奖励 (3项)             │
│ ✓ 晾衣服              +2  14:30 │
│ ✓ 不发脾气            +1  08:00 │
│ ✓ 早读20分钟         +3  07:30 │
├─────────────────────────────────┤
│ 待完成奖励 (5项)                 │
│ ○ 拖地               +3         │
│ ○ 户外运动30分钟     +5         │
│ ○ 复习今天内容       +5         │
└─────────────────────────────────┘
```

### 2. **⭐ 手工补录往期积分**（保持不变）
```
📅 补录入口：
- 底部导航中间突出按钮【➕补录】
- 首页日历图标点击进入"历史记录"

补录功能包括：
- 单日补录（选择任意历史日期）
- 批量补录（日期范围 + 模板复制）
- 补录标记（标注为"补录"状态）
- 补录记录可追溯
```

### 3. **⭐ 积分规则编辑功能（图标随机生成）**

#### A. **规则管理入口**
```
入口位置：【我的】页面 → 【积分规则管理】
```

#### B. **规则列表页面**
```
┌─────────────────────────────────┐
│    ←   积分规则管理        ✚ 新增 │
├─────────────────────────────────┤
│ 🔍 搜索规则...                    │
├─────────────────────────────────┤
│ 📁 分类筛选                        │
│ [全部] [家务] [习惯] [学习] [惩罚]│
├─────────────────────────────────┤
│ 🏷️ 奖励规则 (23项)                 │
│ ┌─────────────────────────────┐ │
│ │ 🧹 晾衣服                    │ │ ← 图标随机生成
│ │   家务类 · +2分             │ │
│ │   [编辑] [禁用] [删除]      │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 📚 早读20分钟                │ │ ← 图标随机生成
│ │   学习习惯 · +3分           │ │
│ │   [编辑] [禁用] [删除]      │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ ⚠️ 惩罚规则 (12项)                 │
│ ┌─────────────────────────────┐ │
│ │ ⏰ 起床拖拉                  │ │ ← 图标随机生成
│ │   日常习惯 · -2分           │ │
│ │   [编辑] [禁用] [删除]      │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

#### C. **新增/编辑规则页面（无图标选择）**
```
┌─────────────────────────────────┐
│    ←   [新增/编辑]规则     ✓ 保存 │
├─────────────────────────────────┤
│ 规则名称 *                       │
│ [晾衣服]                         │
├─────────────────────────────────┤
│ 分类 *                           │
│ [家务类] ▼                       │
├─────────────────────────────────┤
│ 类型 *                           │
│ ○ 奖励  ○ 惩罚                   │
├─────────────────────────────────┤
│ 积分值 *                         │
│ [+2]                             │
├─────────────────────────────────┤
│ 启用状态                         │
│ ☑ 启用                           │
├─────────────────────────────────┤
│ 备注（选填）                     │
│ [每天晾晒衣物]                   │
└─────────────────────────────────┘
```

#### D. **图标随机生成逻辑**
```javascript
// 图标库（预定义40+个常用图标）
const ICON_LIBRARY = [
  '🧹', '🧺', '🍽️', '🧼', '🧽', '🧹', '🧴', '🧻',  // 家务类
  '📚', '✏️', '📖', '🎒', '📝', '🔍', '💡', '⭐',  // 学习类
  '💪', '🏃', '🧘', '🚴', '🏊', '⚽', '🏀', '🎯',  // 运动类
  '😊', '😴', '🍎', '🥗', '🚿', '🧦', '👕', '🧸',  // 习惯类
  '⏰', '📱', '📺', '🎮', '🍪', '🥤', '🍔', '🎪',  // 惩罚/娱乐类
  '🌟', '🎉', '🏆', '🎨', '🎵', '🧩', '🤖', '🐶'   // 通用类
];

// 根据规则分类智能随机
function generateIcon(category: string): string {
  const categoryIcons = {
    '家务': ['🧹', '🧺', '🍽️', '🧼', '🧽'],
    '生活习惯': ['😊', '💪', '🚿', '😴', '🍎'],
    '学习习惯': ['📚', '✏️', '📖', '💡', '⭐'],
    '加分项': ['🌟', '🏆', '🎉', '🎨', '🎵'],
    '惩罚': ['⏰', '📱', '😤', '⚠️', '❌']
  };
  
  const icons = categoryIcons[category] || ICON_LIBRARY;
  return icons[Math.floor(Math.random() * icons.length)];
}
```

#### E. **规则编辑功能细节**
```
📋 支持操作：
- 新增自定义规则（图标自动随机生成）
- 编辑现有规则（名称、分类、分值，图标保持不变）
- 删除规则（需二次确认）
- 启用/禁用规则（禁用后不在记录页显示）
- 拖拽排序（调整显示顺序）
- 批量操作（批量启用/禁用/删除）

🔄 数据同步：
- 规则变更后，历史记录不受影响
- 新规则即时生效
- 支持从备份恢复规则配置
```

### 4. **⭐ 积分兑换功能（支持周期限制）**

#### A. **兑换首页**
```
┌─────────────────────────────────┐
│    🎁 积分商城                    │
├─────────────────────────────────┤
│ 💰 当前积分: 356                  │
├─────────────────────────────────┤
│ 🔥 热门兑换                        │
│  ┌─────────────────────────────┐ │
│  │ 📱 手机时间 30分钟          │ │
│  │ 50积分 ｜ 每周2次           │ │ ← 显示周期限制
│  │ [立即兑换]                   │ │
│  └─────────────────────────────┘ │
│  ┌─────────────────────────────┐ │
│  │ 🥤 饮料 1杯                 │ │
│  │ 30积分 ｜ 每月5次           │ │ ← 显示周期限制
│  │ [立即兑换]                   │ │
│  └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 📦 全部商品                        │
│  ┌─────────────────────────────┐ │
│  │ 📱 手机时间 30分钟    50    │ │
│  │   每周2次                    │ │
│  ├─────────────────────────────┤ │
│  │ 📺 电视时间 30分钟    40    │ │
│  │   每周3次                    │ │
│  ├─────────────────────────────┤ │
│  │ 🥤 饮料 1杯           30    │ │
│  │   每月5次                    │ │
│  ├─────────────────────────────┤ │
│  │ 🎪 游戏厅 1次         80    │ │
│  │   每月2次                    │ │
│  ├─────────────────────────────┤ │
│  │ 🎬 电影院 1次         150   │ │
│  │   每年1次                    │ │ ← 年限制
│  ├─────────────────────────────┤ │
│  │ ✈️ 长途旅行 1次       500   │ │
│  │   每年1次                    │ │ ← 年限制
│  └─────────────────────────────┘ │
└─────────────────────────────────┘
```

#### B. **兑换商品新增/编辑页面**
```
┌─────────────────────────────────┐
│    ←   [新增/编辑]商品     ✓ 保存 │
├─────────────────────────────────┤
│ 商品名称 *                       │
│ [手机时间]                       │
├─────────────────────────────────┤
│ 所需积分 *                       │
│ [50]                             │
├─────────────────────────────────┤
│ 周期限制类型 *                   │
│ ○ 每周  ○ 每月  ○ 每年           │
├─────────────────────────────────┤
│ 周期内限制次数 *                 │
│ [2] 次/周                        │
├─────────────────────────────────┤
│ 图标                             │
│ [📱] (手动选择)                  │
├─────────────────────────────────┤
│ 商品描述                         │
│ [每次30分钟，需家长监督]         │
├─────────────────────────────────┤
│ 启用状态                         │
│ ☑ 启用                           │
└─────────────────────────────────┘
```

#### C. **兑换限额检查逻辑**
```
📊 周期类型及计算方式：
- 每周：按自然周（周一至周日）统计
- 每月：按自然月（1日至月末）统计
- 每年：按自然年（1月1日至12月31日）统计

🔍 兑换前检查：
1. 查询该周期内已兑换次数
2. 比较是否超过周期限制
3. 若未超限，允许兑换并记录
4. 若已超限，提示"本周/月/年已兑换X次，请下个周期再来"
```

#### D. **兑换记录页面（按周期分组）**
```
┌─────────────────────────────────┤
│    ←   兑换记录             │
├─────────────────────────────────┤
│ 📅 2026年3月                      │
│ ┌─────────────────────────────┐ │
│ │ 03-09 手机时间 30分钟   -50 │ │
│ │ 本周已兑: 1/2次             │ │ ← 显示周期进度
│ │ 状态：已使用 ✓              │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 03-08 饮料 1杯         -30  │ │
│ │ 本月已兑: 3/5次             │ │ ← 显示周期进度
│ │ 状态：待使用 ⏳              │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 03-01 电影院 1次      -150  │ │
│ │ 今年已兑: 1/1次             │ │ ← 年限制进度
│ │ 状态：已使用 ✓              │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 📊 本月累计兑换：280积分         │
└─────────────────────────────────┘
```

### 5. **👤 我的页面（备份功能移入）**

```
┌─────────────────────────────────┐
│    👤 我的                        │
├─────────────────────────────────┤
│  👨‍👩‍👧‍👦 家庭成员                     │
│  小明 (8岁) [切换孩子] [添加]    │
├─────────────────────────────────┤
│  ⚙️ 规则与兑换管理                 │
│  • 📋 积分规则管理    [编辑]      │ ← 规则编辑入口
│  • 🎁 兑换商品管理    [编辑]      │ ← 兑换配置入口
├─────────────────────────────────┤
│  💾 数据备份与恢复                │ ← 备份功能移入
│  ┌─────────────────────────────┐ │
│  │ 本地存储: 2.3MB/可用空间大   │ │
│  │ 最后备份: 今天 14:30         │ │
│  ├─────────────────────────────┤ │
│  │ [📤 手动备份]                │ │
│  │ [📥 从文件恢复]              │ │
│  │ [⚡ 自动备份设置]             │ │
│  │ [📋 备份历史]                │ │
│  └─────────────────────────────┘ │
├─────────────────────────────────┤
│  📊 数据统计                       │
│  • 年度积分报告                    │
│  • 孩子成长曲线                    │
│  • 兑换分析报表                    │
├─────────────────────────────────┤
│  ⚙️ 系统设置                       │
│  • 深色模式          [自动] ▼     │
│  • 通知提醒          [开启]        │
│  • 语言              [简体中文]   │
├─────────────────────────────────┤
│  📖 帮助与反馈                    │
│  • 新手指南                        │
│  • 常见问题                        │
│  • 意见反馈                        │
│  • 关于我们                        │
└─────────────────────────────────┘
```

---

## 🧠 **三、技术实现要求**

### 1. **数据模型扩展**

```javascript
// 规则模型（图标自动生成）
interface Rule {
  id: string,
  name: string,
  category: '家务' | '生活习惯' | '学习习惯' | '加分项' | '惩罚',
  type: 'reward' | 'penalty',
  points: number,
  icon: string,              // 新增时随机生成，编辑时不修改
  enabled: boolean,
  sortOrder: number,
  description?: string,
  createdAt: string,
  updatedAt: string
}

// 兑换商品模型（增加周期限制）
interface RewardItem {
  id: string,
  name: string,
  points: number,
  icon: string,
  description: string,
  cycleType: 'weekly' | 'monthly' | 'yearly',  // 周期类型
  cycleLimit: number,                           // 周期内限制次数
  enabled: boolean,
  sortOrder: number
}

// 兑换记录模型
interface ExchangeRecord {
  id: string,
  itemId: string,
  itemName: string,
  points: number,
  quantity: number,
  totalPoints: number,
  exchangeDate: string,
  cycleType: string,        // 记录当时的周期类型
  cycleLimit: number,       // 记录当时的限制次数
  status: 'pending' | 'used' | 'expired',
  usedDate?: string,
  note?: string,
  childId: string
}
```

### 2. **核心功能代码要求**

#### A. **规则图标随机生成**
```javascript
// 图标随机生成器
const IconGenerator = {
  // 图标库（可按需扩展）
  icons: {
   家务: ['🧹', '🧺', '🍽️', '🧼', '🧽', '🧴', '🧻', '🧤'],
    生活习惯: ['😊', '💪', '🏃', '🚿', '😴', '🍎', '🥗', '🚴'],
    学习习惯: ['📚', '✏️', '📖', '🎒', '📝', '🔍', '💡', '⭐'],
    加分项: ['🌟', '🏆', '🎉', '🎨', '🎵', '🎯', '🎪', '🤹'],
    惩罚: ['⏰', '📱', '😤', '⚠️', '❌', '😠', '🔇', '🚫']
  },
  
  // 生成随机图标
  generate(category: string): string {
    const categoryIcons = this.icons[category] || this.icons['生活习惯'];
    return categoryIcons[Math.floor(Math.random() * categoryIcons.length)];
  },
  
  // 批量生成（用于初始化）
  batchGenerate(rules: Array<{category: string}>): Array<string> {
    return rules.map(rule => this.generate(rule.category));
  }
};
```

#### B. **周期限制检查**
```javascript
// 周期限制检查器
class CycleLimiter {
  // 检查是否可兑换
  async canExchange(
    itemId: string,
    childId: string,
    date: Date = new Date()
  ): Promise<{
    canExchange: boolean,
    used: number,
    limit: number,
    remaining: number,
    message: string
  }> {
    const item = await getRewardItem(itemId);
    const records = await getExchangeRecords(itemId, childId);
    
    // 根据周期类型筛选记录
    const periodRecords = this.filterByCycle(records, item.cycleType, date);
    const used = periodRecords.length;
    
    return {
      canExchange: used < item.cycleLimit,
      used,
      limit: item.cycleLimit,
      remaining: item.cycleLimit - used,
      message: this.getMessage(item.cycleType, used, item.cycleLimit)
    };
  }
  
  // 按周期筛选
  filterByCycle(
    records: ExchangeRecord[],
    cycleType: string,
    date: Date
  ): ExchangeRecord[] {
    const startDate = this.getCycleStartDate(cycleType, date);
    return records.filter(r => new Date(r.exchangeDate) >= startDate);
  }
  
  // 获取周期起始日期
  getCycleStartDate(cycleType: string, date: Date): Date {
    const d = new Date(date);
    switch(cycleType) {
      case 'weekly':
        // 回到本周一
        const day = d.getDay();
        const diff = d.getDate() - day + (day === 0 ? -6 : 1);
        return new Date(d.setDate(diff));
      case 'monthly':
        // 回到本月1号
        return new Date(d.getFullYear(), d.getMonth(), 1);
      case 'yearly':
        // 回到本年1月1号
        return new Date(d.getFullYear(), 0, 1);
      default:
        return new Date(0); // 全部
    }
  }
  
  // 获取提示信息
  getMessage(cycleType: string, used: number, limit: number): string {
    const cycleNames = {
      weekly: '本周',
      monthly: '本月',
      yearly: '今年'
    };
    return `${cycleNames[cycleType]}已兑换${used}/${limit}次`;
  }
}
```

#### C. **规则编辑模块**
```javascript
// 规则CRUD操作
async function createRule(ruleData: Omit<Rule, 'id' | 'icon'>) {
  // 自动生成图标
  const icon = IconGenerator.generate(ruleData.category);
  
  const newRule = {
    ...ruleData,
    id: generateId(),
    icon,
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString()
  };
  
  await saveRule(newRule);
  return newRule;
}

async function updateRule(id: string, updates: Partial<Rule>) {
  // 注意：不更新icon字段
  const { icon, ...safeUpdates } = updates;
  return await updateRuleData(id, safeUpdates);
}
```

### 3. **UI组件代码要求**
- 规则列表（带图标、分类标签、操作按钮）
- 新增规则页面（无图标选择）
- 兑换商品卡片（显示周期限制）
- 兑换记录（显示周期进度条）
- 周期选择器（每周/每月/每年）

---

## 📋 **四、完整交付要求**

请提供：

### 1. **代码部分**
- 规则编辑模块（含图标随机生成）
- 兑换模块（含周期限制检查）
- 备份功能组件（移入我的页面）
- 所有页面UI组件（Vue/React）
- 现代化样式表（含深色模式）

### 2. **功能流程图**
- 规则新增流程（图标自动生成）
- 兑换流程（周期检查 → 扣积分 → 记录）
- 周期限制计算逻辑
- 备份恢复流程

### 3. **数据模型定义**
- 规则模型（Rule，含icon自动生成）
- 兑换商品模型（RewardItem，含周期类型）
- 兑换记录模型（ExchangeRecord）
- 备份文件结构

### 4. **图标库列表**
- 预定义的40+个图标
- 按分类的图标映射表
- 随机生成算法说明

---

## ⚠️ **五、注意事项**

1. **图标随机生成**
   - 同一分类下尽量分散
   - 编辑规则时不修改图标
   - 备份恢复时保留原图标

2. **周期限制计算**
   - 注意时区处理
   - 跨年/跨月边界条件
   - 并发兑换时的锁机制

3. **用户体验**
   - 兑换前显示剩余次数
   - 达到限制时禁用按钮
   - 显示下次可用时间

4. **数据一致性**
   - 周期统计准确
   - 积分扣除原子性
   - 兑换记录可追溯

---

## 🎯 **最终效果预期**

一个**功能完整、界面现代、易于使用**的家庭积分管理系统，具备：
- ✅ 清新卡片式设计，流畅动画
- ✅ **自定义积分规则编辑（图标自动随机生成）**
- ✅ **完整积分商城兑换系统（支持每周/每月/每年周期限制）**
- ✅ 手工补录历史积分
- ✅ **备份功能整合在【我的】页面**
- ✅ 可视化统计报表
- ✅ 深色模式自适应